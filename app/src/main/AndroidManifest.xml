<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="eduardo.souza.io.marvelappstarter">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="eduardo.souza.io.marvelappstarter.MarvelApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app_punish"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_app_punish_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarvelAppStarter"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name="eduardo.souza.io.marvelappstarter.ui.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="eduardo.souza.io.marvelappstarter.ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>
    </application>

</manifest>